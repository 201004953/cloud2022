spring:
  profiles:
    active: prod
---
spring:
  profiles: prod
  application:
    name: config
  cloud:
    config:
      uri: http://localhost:8001
      label: master
      profile: prod

---
spring:
  profiles: test
  application:
    name: config
  cloud:
    config:
      uri: http://localhost:8001
      profile: test
      label: master